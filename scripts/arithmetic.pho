fn inc(n: Number): Number {
    return n + 1;
}

fn add(a: Number, n: Number): Number {
    return a + n;
}

fn mean(a: Number, b: Number): Number {
    let sum: Number = a + b;
    return sum / 2;
}
